import { Campaign } from "../types/campaign";

export function parseCampaign(campaign): Campaign {
  const parsed: Campaign = new Campaign();
  return parsed;
}

export function parseCampaigns(campaign): Array<Campaign> {
  const parsed: Array<Campaign> = [];
  return parsed;
}
