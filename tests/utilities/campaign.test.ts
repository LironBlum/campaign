//import {  } from "../../src/services/campaign";
//import {  } from "../../src/utilities/campaign";

describe("? Tests", () => {
  test("should ?", () => {
    const input = {};
    const result = null; //call the tested function
    expect(result).toEqual({});
  });
});
